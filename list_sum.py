def list_sum(arr):
    """
    Вычисляет сумму всех элементов в списке рекурсивно.

    :param arr: Список чисел.
    :return: Сумма всех элементов в списке.
    """
    if not arr:  # Базовый случай: пустой список
        return 0
    return arr[0] + list_sum(arr[1:])  # Рекурсивный случай